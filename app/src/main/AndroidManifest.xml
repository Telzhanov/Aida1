<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="beks.androidcourse.kz.aida">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.LoginScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SplashActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".controller.RegisterScreen" />
        <activity
            android:name=".controller.ApplicationScreenActivity"
            android:label="@string/title_activity_application_screen" />
        <activity android:name=".controller.ExampleRecyclerActivity" />
        <activity android:name=".controller.SplashScreen" />
        <activity android:name=".controller.CreditList" />
        <activity android:name=".controller.ApplicationActivity" />
        <activity android:name=".controller.PaymentsClass" />
        <activity android:name=".controller.ApplRegistraionActivity" />
        <activity
            android:name=".controller.SicksApplication"
            android:label="@string/title_activity_sicks_application"></activity>
        <activity android:name=".controller.BlogView"/>
    </application>

</manifest>